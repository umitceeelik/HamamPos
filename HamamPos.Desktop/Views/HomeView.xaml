<UserControl x:Class="HamamPos.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button Content="POS"     Width="180" Height="80" Margin="8" Command="{Binding GoPosCommand}"/>
            <Button Content="Yönetim" Width="180" Height="80" Margin="8" Command="{Binding GoAdminCommand}"/>
            <Button Content="Raporlar" Width="180" Height="80" Margin="8" Command="{Binding GoReportsCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>